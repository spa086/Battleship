"/whatsUp" 
Request model:
{
	SessionId - integer.
}
Example request body JSON:
{"SessionId":144}
Returns one of the following strings:
    "WaitingForStart",
    "CreatingShips",
    "YourTurn",
    "OpponentsTurn".


"/createFleet"
Request model:
{
	SessionId - integer.
	Decks - integer array.
}
Example request body JSON:
{"SessionId":144,"Decks":[1,4,5]}
Returns nothing.

"/attack"
Request model:
{
	SessionId - integer.
	Location - integer.
}
Returns nothing.