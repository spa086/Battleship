"/whatsUp" 
Request model:
{
	userId - int.
}
JSON Request body examples:
{"userId":144}
Returns object: 
{
    state - string enum { "WaitingForStart", "CreatingFleet", "YourTurn", "OpponentsTurn" }
	userId - int optional
}


"/createFleet"
Request model:
{
	userId - integer.
	decks - integer array.
}
Example request body JSON:
{"userId":144,"ships":[]}

"/attack"
Request model:
{
	SessionId - integer.
	Location - integer.
}
Returns object:
{
	Result - string,
	Player1Fleet - array of objects:
	{
		Decks - array of objects: 
		{
			Location - integer.
			Destroyed - boolean.
		}
	}
	Player2Fleet - array of objects:
	{
		Decks - array of objects: 
		{
			Location - integer.
			Destroyed - boolean.
		}
	}
}
"Result" property can be one of the following strings:
    "Hit",
    "Killed",
    "Missed",
    "Win".

"/abort"
Request model:
{
	SessionId - integer.
}
Returns nothing.
