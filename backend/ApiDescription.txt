"/whatsUp" 
Int:Id
Returns enum (converted to string in JSON):
enum WhatsUpResponse
{
    WaitingForStart,
    CreatingShips,
    YourTurn,
    OpponentsTurn
}

"/createFleet"
Parameter:
ShipTransporrModel[]
class ShipTransporrModel:
{
    int[] Decks
}
Returns nothing.

"/attack"
Parameter:
Object of class LocationTransportModel
class LocationTransportModel:
{
    int X
	int Y
}
Returns nothing.